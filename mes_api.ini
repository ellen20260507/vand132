[MesApi]
; API Key：客户 MES 请求头 X-Api-Key 需与此一致
; 留空表示不校验（仅建议本机调试时使用）
apiKey=

; POST /api/mes/readings/query 分页说明：
; pageSize 省略时默认 1000，无上限，客户发多少用多少
; pageSize 为 "ALL" 时不分页，一次返回全部符合条件的 items（忽略 page）
