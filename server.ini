[Server]
port=1388                  ; 端口必须是1-65535之间的整数（推荐1388、8080等）
host=0.0.0.0             ; 监听的IP地址（空值表示监听所有网络接口）
minThreads=1               ; 可选：最小线程数
maxThreads=10              ; 可选：最大线程数
timeout=60000              ; 可选：超时时间（毫秒）
maxRequestSize=16384       ; 可选：最大请求大小（字节）